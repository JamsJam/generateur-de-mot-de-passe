<h1>Bonjour</h1>

<p>Pour réinitialiser votre mot de passe clicker sur le lien suivant</p>
<br>

<a href="{{ reset_url ~ "/" ~ resetToken.token}}">{{ reset_url ~ "/" ~ resetToken.token}}</a>
<br>
<p>Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>En cas de problème contacter votre administrateur.</p>
