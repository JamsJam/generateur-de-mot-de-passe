{% extends 'base.html.twig' %}

{% block title %}Motdepasse index{% endblock %}

{% block body %}
    <h1>Motdepasse index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Website</th>
                <th>Username</th>
                <th>Password</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for motdepasse in motdepasses %}
            <tr>
                <td>{{ motdepasse.id }}</td>
                <td>{{ motdepasse.website }}</td>
                <td>{{ motdepasse.username }}</td>
                <td>{{ motdepasse.password }}</td>
                <td>
                    <a href="{{ path('app_motdepasse_show', {'id': motdepasse.id}) }}">show</a>
                    <a href="{{ path('app_motdepasse_edit', {'id': motdepasse.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_motdepasse_new') }}">Create new</a>
{% endblock %}
