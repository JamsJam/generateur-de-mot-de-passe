{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}

<div class="show-container">

    <h1 class="titre">User</h1>

    <div class="card-wrapper">

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ user.id }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ user.email }}</td>
                </tr>
                <tr>
                    <th>Roles</th>
                    <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                </tr>
                <tr>
                    <th>Password</th>
                    <td>{{ user.password }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ user.nom }}</td>
                </tr>
                <tr>
                    <th>Prenom</th>
                    <td>{{ user.prenom }}</td>
                </tr>
            </tbody>
        </table>

        <div class="button-wrapper">
            <a class="btn btn-modal" href="{{ path('app_user_index') }}">Retour</a>

            <a class="btn btn-modal-primary" href="{{ path('app_user_edit', {'id': user.id}) }}">Éditer</a>
        </div>
    </div>

    {{ include('user/_delete_form.html.twig') }}
</div>
{% endblock %}
