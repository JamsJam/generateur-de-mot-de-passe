<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="{{asset("build/app.css")}}">
        <title>{% block title %}Password Manager | {% endblock %}</title>
    </head>
    <body>
        
        <main class="main-gradient">
        <header>
        <nav>
            <div class="logo-wrapper">
            <a href="{{path('app_home')}}">
              <img class="logo" src="{{asset("/image/logo_okai_blanc.png")}}" alt="">
            </a>
            </div>
            {% if (app.user) %}
                <a class="" href="{{path('app_logout')}}"><img class=" logout-icon" src="{{asset("/image/logout.png")}}" alt=""></a>
            {% else %}
               
            {% endif %}
        </nav>
        </header>
        
            
            {% block body %}

            {% endblock %}
            
        </main>
        
        <footer>
            <a class="mentions" href="">Mentions légales</a>
        </footer>
        <script src="{{asset("/js/custom.js")}}"></script>
        {% block script %}{% endblock %}
    </body>
</html>
