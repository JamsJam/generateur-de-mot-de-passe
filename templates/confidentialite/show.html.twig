{% extends 'base.html.twig' %}

{% block title %}Confidentialite{% endblock %}

{% block body %}
    <h1>Confidentialite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ confidentialite.id }}</td>
            </tr>
            <tr>
                <th>Acces</th>
                <td>{{ confidentialite.acces }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_confidentialite_index') }}">Retour</a>

    <a href="{{ path('app_confidentialite_edit', {'id': confidentialite.id}) }}">Modifier</a>

    {{ include('confidentialite/_delete_form.html.twig') }}
{% endblock %}
